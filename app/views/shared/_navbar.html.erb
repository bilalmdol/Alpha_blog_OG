<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <button class="btn btn-success">bilal</button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Home</a>
                </li>
                
                <% if isLoggedIn? %>
                    <li class="nav-item">
                        <% if current_user.admin? %>
                            Admin-
                        <% end %>
                        <%= link_to current_user.username, user_path(current_user), method: :delete %>
                    </li>
                    <li class="nav-item">
                        <%= button_to "logout", logout_path, method: :delete %>
                    </li>

                    <li class="nav-item">
                        <%= button_to "Delete account", user_path(current_user), method: :delete, data: { turbo_confirm: "all articles asssoc will also be deleted, Are you sure?" } %>
                    </li>
               
                <% else %>
                     <li class="nav-item">
                        <%= link_to "login", login_path %>
                     </li>
                     |    |
                    <li class="nav-item">
                            <%= link_to "sign up", signup_path %>
                    </li>
                <% end %>


                <% if isLoggedIn? && current_user.admin? %>
                    <li class="nav-item">
                        <%= link_to "(root) Create new Category", new_category_path %>
                    </li>|
                    <li class="nav-item">
                        <%= link_to "View Categories", categories_path %>
                    </li>
               |
                <% else %>
                      <li class="nav-item">
                        <%= link_to "View Categories", categories_path %>
                    </li>
                <% end %>
        
                
                
            </ul>
            <form class="d-flex" role="search">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>